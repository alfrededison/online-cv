export enum ActionTypes {
  // User reducer types
  SET_AUTHENTICATED,
  SET_UNAUTHENTICATED,
  SET_USER,
  // UI reducer types
  SET_ERRORS,
  CLEAR_ERRORS,
  // Resume reducer types
  LOAD_RESUME,
}
